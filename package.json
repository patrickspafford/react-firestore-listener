{
  "name": "react-firestore-listener",
  "version": "1.0.0",
  "description": "A React hook to easily keep an up-to-date value of one or more Firestore documents in state for use in your application.",
  "main": "index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "files": [
    "dist"
  ],
  "author": "Patrick Spafford",
  "license": "ISC",
  "dependencies": {
    "firebase": "^8.6.1",
    "react": "^17.0.2"
  },
  "devDependencies": {
    "@types/react": "^17.0.6",
    "typescript": "^4.2.4"
  }
}
